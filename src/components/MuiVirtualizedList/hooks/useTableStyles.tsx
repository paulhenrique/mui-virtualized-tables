import { makeStyles } from "@material-ui/core";

export const useTableStyles = makeStyles({
  table: {
    display: "flex",
    flexDirection: "column",
  },
  tableBody: {
    display: "flex",
  },
});
